#include <stdio.h>
/**
 * main - Entry
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char b;

	char u;

	b = 'a';
	u = 'A';
	while
		(b <= 'z')
		{
			putchar(b);
			b++;
		}
	while
		(u <= 'Z')
		{
			putchar(u);
			u++;
		}
	putchar('\n');
	return (0);
}
